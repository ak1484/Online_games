# Contributing

- Use markdown files under `agents/` for workflow coordination.
- Keep `prod_games/` for live-ready outputs only.
- Keep `dev/` for active development and test notes.
- Update `plan.md` with any changes to the agent workflow.
